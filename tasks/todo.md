# Real-world example clean parse

- [x] Reproduce and classify remaining failures.
- [x] Parse branch-local statements in conditional procedure declarations.
- [x] Add focused corpus coverage and preserve existing conditional-header CSTs.
- [x] Record the public CST contract in ADR 0003.
- [ ] Design a non-regressing representation for directive-split `If` constructs.
- [ ] Preserve whitespace when distinguishing implicit member arguments from qualified callees.
- [ ] Support `ReDim indexedReceiver(i).member(...)` without changing ordinary call parsing.
- [ ] Reach a clean parse across all imported examples.
